/**
 * DEPRECATED — Merged into workers/api.ts
 *
 * All /api/* routes (including POST /api/submit) are now handled
 * by the single Worker in api.ts. This file is kept as a reference
 * and will be removed once deployment is verified.
 *
 * See workers/api.ts → handleSubmit()
 */
